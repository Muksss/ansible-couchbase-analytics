---
# defaults file for analytics
analytics_version: 1.0.0-200
analytics_basedir: analytics
analytics_logdir: "{{ analytics_basedir }}/logs"
analytics_installdir: "{{ analytics_basedir }}/install"
analytics_confdir: "{{ analytics_basedir }}/conf"
